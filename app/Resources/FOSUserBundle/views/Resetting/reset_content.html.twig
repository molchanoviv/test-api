<form action="{{ path('fos_user_resetting_reset', {'token': token}) }}" {{ form_enctype(form) }} method="POST" class="fos_user_resetting_reset form-inline">
    <div class="form-group">
        {{ form_widget(form) }}
    </div>
    <input type="submit" class="btn btn-default" value="{{ 'resetting.reset.submit'|trans({}, 'FOSUserBundle') }}"/>
</form>
